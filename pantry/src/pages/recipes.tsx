import React from "react";
import { RecipeInterface } from "./../interfaces";

const Recipes = () => {
  // array of recipes used to store data
  interface RecipeArray extends Array<RecipeInterface> {}

  return <h1>Recipes!!!!</h1>;
};

export default Recipes;
